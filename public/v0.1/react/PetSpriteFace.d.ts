import { ResolvedPet } from '../core/types';
interface Props {
    active: ResolvedPet;
    className?: string;
    size?: number;
    rowId?: string;
}
export declare function PetSpriteFace({ active, className, size, rowId }: Props): import("react/jsx-runtime").JSX.Element;
export {};
